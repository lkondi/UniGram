In order to be able to compile the project, you need to install cocoa pods. You can use these commands :

$ sudo gem install cocoapods
$ pod install

After installing the updates, make sure to open the .xcworkspace file instead of the .xcodeproj.